# Proyecto Kratos — ERP Municipal SelvaTic
# PUERTO FIJO: 5174 — no cambiar nunca (ver .cursor/rules/puerto-fijo.mdc)

name: kratos

services:
  kratos-frontend:
    container_name: kratos-frontend
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "5174:80"
    restart: unless-stopped
